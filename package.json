{
  "name": "dima-plugin-chrome",
  "version": "2.0.0",
  "private": true,
  "description": "Plugin Chrome DIMA (Digital Influence Manipulation Analyzer) — M82 Project",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "ci": "npm run lint && npm test"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "happy-dom": "^15.11.7",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
